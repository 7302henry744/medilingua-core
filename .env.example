# Core Configuration
PROJECT_NAME=medilingua-core
ENVIRONMENT=development

# Backend (Python/FastAPI)
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0
# Security: In production, this must be a long random string
SECRET_KEY=dev_secret_key_change_in_production_12345

# Frontend (React/Vite)
FRONTEND_PORT=3000
# URL to access the backend from the browser
VITE_API_URL=http://localhost:8000